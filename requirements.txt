# Core (stdlib-only fallbacks exist for all of these -- install for full functionality)
ddgs>=9.0.0
requests>=2.31.0
PyMuPDF>=1.24.0
python-dotenv>=1.0.0

# Real LLM backend actually used/tested (Gemini). MockLLM is used automatically
# if no key is set, so this is only required for a live run.
google-genai>=1.0.0

# Optional alternate LLM backends (untested against this codebase, but the
# provider-dispatch in llm.py supports them if keyed)
# openai>=1.0.0
# anthropic>=0.30.0

# Browser control -- real headless-browser fetch for JS-rendered pages
# (falls back to plain requests.get() automatically if not installed)
browser-use>=0.13.0

# Web UI (python -m shutdown.web). These arrive transitively with browser-use
# too, but the UI shouldn't silently break for anyone who skips that install.
starlette>=0.37.0
uvicorn>=0.30.0
sse-starlette>=2.0.0
python-multipart>=0.0.9
