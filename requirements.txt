# Core (stdlib-only fallbacks exist for all of these -- install for full functionality)
duckduckgo-search>=6.0.0
requests>=2.31.0
PyMuPDF>=1.24.0

# Optional real LLM backends (pick one; MockLLM is used if none are installed/keyed)
# google-generativeai>=0.7.0
# openai>=1.0.0
# anthropic>=0.30.0

# Optional browser control (heavier install, needs Playwright browsers)
# browser-use>=0.1.0
