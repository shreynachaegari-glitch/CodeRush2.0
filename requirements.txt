# Core (stdlib-only fallbacks exist for all of these -- install for full functionality)
ddgs>=9.0.0
requests>=2.31.0
PyMuPDF>=1.24.0
python-dotenv>=1.0.0

# Real LLM backends actually used/tested. MockLLM is used automatically if no
# key is set, so these are only required for a live run.
google-genai>=1.0.0
# NVIDIA NIM speaks the OpenAI wire format, so the fallback chain in llm.py
# (Gemini -> NVIDIA/DeepSeek -> a second NVIDIA key) needs this installed,
# not just OpenAI's own API -- uncommented since that fallback is real now.
openai>=1.0.0

# Optional alternate LLM backend (untested against this codebase, but the
# provider-dispatch in llm.py supports it if keyed)
# anthropic>=0.30.0

# Browser control -- real headless-browser fetch for JS-rendered pages
# (falls back to plain requests.get() automatically if not installed)
browser-use>=0.13.0

# Web UI (python -m shutdown.web). These arrive transitively with browser-use
# too, but the UI shouldn't silently break for anyone who skips that install.
starlette>=0.37.0
uvicorn>=0.30.0
sse-starlette>=2.0.0
python-multipart>=0.0.9
